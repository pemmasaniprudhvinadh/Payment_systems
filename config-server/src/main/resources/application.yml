server:
  port: 8888

spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        bootstrap: false
        composite:
          - type: native
            search-locations:
              - classpath:/config-repo
              - file:./config-repo

management:
  endpoints:
    web:
      exposure:
        include: health,info,env
